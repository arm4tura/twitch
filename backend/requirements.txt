# =============================================================================
# УСТАРЕЛО. Используйте requirements-gpu.txt (см. README → Установка).
#
# Причина: тут были неpin-нутые версии, из-за которых у пользователей ставились
# разные наборы torch/pyannote/speechbrain, что ломало monkey-patches в
# transcription.py. Полный воспроизводимый lock — в requirements-gpu.txt.
# =============================================================================

-r requirements-gpu.txt
