# Desktop placeholder

Electron + React UI будет добавлен в следующих фазах:

- Фаза 4: FastAPI/WebSocket backend wrapper;
- Фаза 5: Electron UI импорт → прогресс → таймлайн → экспорт;
- Фаза 6: сборка установщика.

В Фазе 1 рабочая часть находится в `../backend/src/twitch_cut`.
